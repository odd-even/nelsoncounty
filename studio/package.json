{
  "name": "check-into-nelson-studio",
  "private": true,
  "version": "1.0.0",
  "description": "Sanity Studio for Check Into Nelson — Odd + Even",
  "scripts": {
    "dev": "sanity dev",
    "start": "sanity dev",
    "build": "sanity build",
    "deploy": "sanity deploy",
    "seed": "node scripts/seed.mjs"
  }
}
