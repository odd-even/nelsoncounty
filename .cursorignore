# Reduce Cursor indexing to lower memory use and avoid crashes (code: 5)
# These paths are excluded from search/indexing.

# Backup folders and old code
backups/
Old/

# CSV: exclude folder to avoid indexing many large CSVs and reports
# Remove this block if you need Cursor to search inside CSV often.
CSV/

# Merge/version control temp files
MERGE_HEAD
MERGE_MODE
MERGE_MSG
REVERT_HEAD
AUTO_MERGE

# Dependencies (if any)
node_modules/
